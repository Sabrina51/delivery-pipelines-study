apiVersion: tekton.dev/v1alpha1
kind: Task
metadata:
  name: cypress-task
spec:
  workspaces:
    - name: workspace
  steps:
    - name: install-cypress
      image: ubuntu
      script: |
        #!/bin/bash
        ls
        ls -la $(workspaces.workspace.path)
        ls -la $(workspaces.root.path)
        ls -la $(workspaces.home.path)
        ls -la $(workspaces.tekton.path)
        ls -la $(workspaces.usr.path)
