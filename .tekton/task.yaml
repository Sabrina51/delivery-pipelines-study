apiVersion: tekton.dev/v1alpha1
kind: Task
metadata:
  name: cypress-task
spec:
  workspaces:
    - name: cypressworkspace
  steps:
    - name: install-cypress
      image: ubuntu
      script: |
        #!/bin/bash
        ls
        ls -la $(cypressworkspace.workspace.path)
